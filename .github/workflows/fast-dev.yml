name: GhostKernel 2.0 Fast Dev APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ghostkernel2-dev-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: false

      - name: Make Gradle executable
        run: chmod +x gradlew

      - name: Verify KornDog signing key
        run: |
          test -f app/korndog-release.keystore
          keytool -list             -keystore app/korndog-release.keystore             -alias korndog             -storepass "KornDog2024!" >/dev/null
          echo "KornDog signing key verified"

      - name: Build signed GhostKernel GitHub debug APK
        env:
          SIGNING_STORE_FILE: ${{ github.workspace }}/app/korndog-release.keystore
          SIGNING_STORE_PASSWORD: "KornDog2024!"
          SIGNING_KEY_ALIAS: "korndog"
          SIGNING_KEY_PASSWORD: "KornDog2024!"
          KORNOS_SYNC_URL: ${{ secrets.KORNOS_SYNC_URL }}
          KORNOS_SYNC_KEY: ${{ secrets.KORNOS_SYNC_KEY }}
        run: ./gradlew :app:assembleGithubDebug --build-cache --parallel

      - name: Find and name APK
        run: |
          APK=$(find app/build/outputs/apk -type f -name "*.apk" | grep -i github | grep -i debug | head -1)

          if [ -z "$APK" ]; then
            echo "GitHub debug APK not found"
            find app/build/outputs -type f -name "*.apk" -print
            exit 1
          fi

          OUTPUT="GhostKernel-2.0-dev-${GITHUB_RUN_NUMBER}.apk"
          cp "$APK" "$OUTPUT"
          echo "OUTPUT_APK=$OUTPUT" >> "$GITHUB_ENV"

          echo "Built:"
          ls -lh "$OUTPUT"

      - name: Upload GhostKernel 2.0 APK
        uses: actions/upload-artifact@v4
        with:
          name: GhostKernel-2.0-dev-${{ github.run_number }}
          path: ${{ env.OUTPUT_APK }}
          if-no-files-found: error
